// a) Arrow function isEven
const isEven = n => n % 2 === 0;

// b) Convert to ternary
const result = marks >= 35 ? "Pass" : "Fail";

// c) Arrow function with ternary
const greet = name => name ? `Hello, ${name}` : "Guest";